<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
	xmlns:http="http://www.mulesoft.org/schema/mule/http"
	xmlns:ee="http://www.mulesoft.org/schema/mule/ee/core"
	xmlns:doc="http://www.mulesoft.org/schema/mule/documentation"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
		http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd
		http://www.mulesoft.org/schema/mule/ee/core http://www.mulesoft.org/schema/mule/ee/core/current/mule-ee.xsd">

	<!--
		OCP BEFORE: every new payment method means editing this choice router, which is shared by
		all the existing methods. The flow is open for modification, which is what OCP tries to avoid.
	-->
	<flow name="ocp-before-process-payment">
		<http:listener doc:name="Listener" config-ref="http-listener-config" path="/ocp/before/payments" allowedMethods="POST">
			<http:response statusCode="#[vars.httpStatus default 200]" />
		</http:listener>
		<choice doc:name="Payment method">
			<when expression="#[payload.paymentMethod == 'creditCard']">
				<flow-ref doc:name="Credit card" name="ocp-before-process-credit-card" />
			</when>
			<when expression="#[payload.paymentMethod == 'bankSlip']">
				<flow-ref doc:name="Bank slip" name="ocp-before-process-bank-slip" />
			</when>
			<otherwise>
				<raise-error type="APP:BAD_REQUEST" description="#['Unsupported payment method: ' ++ (payload.paymentMethod default 'none')]" />
			</otherwise>
		</choice>
	</flow>

	<sub-flow name="ocp-before-process-credit-card">
		<ee:transform doc:name="creditCard payment">
			<ee:message>
				<ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{
	paymentId: "CC-" ++ payload.orderId,
	method: "creditCard",
	amount: payload.amount,
	status: "AUTHORISED"
}]]></ee:set-payload>
			</ee:message>
		</ee:transform>
	</sub-flow>

	<sub-flow name="ocp-before-process-bank-slip">
		<ee:transform doc:name="bankSlip payment">
			<ee:message>
				<ee:set-payload><![CDATA[%dw 2.0
output application/json
---
{
	paymentId: "BS-" ++ payload.orderId,
	method: "bankSlip",
	amount: payload.amount,
	status: "PENDING"
}]]></ee:set-payload>
			</ee:message>
		</ee:transform>
	</sub-flow>
</mule>
